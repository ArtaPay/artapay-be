# =====================================================
# PAYMASTER SIGNER BACKEND CONFIGURATION
# =====================================================

# ⚠️ PENTING: Ini butuh PRIVATE KEY, bukan ADDRESS!
# Private key digunakan untuk menandatangani (sign) data.
# Address tidak bisa dipakai untuk sign - hanya untuk identifikasi.

# Private key dari wallet signer (dimulai dengan 0x)
# Wallet ini HARUS sudah di-add ke Paymaster contract via:
#   paymaster.addSigner(walletAddress)
PAYMASTER_SIGNER_PRIVATE_KEY=

# Server port
PORT=3001

# Allowed CORS origins (frontend URL)
CORS_ORIGINS=http://localhost:5173